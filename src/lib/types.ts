export interface MenuItem {
  name: string;
  description: string;
  price: number;
  image: string;
}

export interface ContactInfo {
  name: string;
  address: string;
  phone: string;
  hours: string;
}
